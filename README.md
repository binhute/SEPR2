# Sampling test branch

This branch periodically uploads sensor sampling data to Firebase every minute for testing and performance evaluation — you can adjust the sampling interval in **`main.cpp`**